// Auto-generated settings page entrypoint
import React from 'react';
import ReactDOM from 'react-dom/client';
__ADAPTER_IMPORT__
import './style.css';

const root = document.getElementById('root');
if (root) {
    ReactDOM.createRoot(root).render(
        <React.StrictMode>
            <SettingsForm />
        </React.StrictMode>
    );
}
